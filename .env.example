# Copy to .env.local for local dev: cp .env.example .env.local
# On Vercel: Project Settings → Environment Variables

NVIDIA_API_KEY=
NVIDIA_MODEL=z-ai/glm-5.1
NVIDIA_MAX_TOKENS=1200

# Required: GitHub PAT with public repository read
GITHUB_TOKEN=
